const { PrismaClient } = require('@prisma/client'); const bcrypt = require('bcrypt'); const prisma = new PrismaClient(); async function main() { const users = await prisma.user.findMany({ select: { email: true, role: true, isApproved: true, passwordHash: true } }); console.log(JSON.stringify(users, null, 2)); const admin = users.find(u => u.email === 'admin@hireloop.com'); if (admin) { console.log('Admin password match:', await bcrypt.compare('Admin123!', admin.passwordHash)); } } main().catch(console.error).finally(() => prisma.$disconnect());
