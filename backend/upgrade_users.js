const { PrismaClient } = require("@prisma/client"); const prisma = new PrismaClient(); async function main() { await prisma.user.updateMany({ data: { isApproved: true, role: "SUPERADMIN" } }); console.log("All existing users upgraded to SUPERADMIN and approved."); } main().catch(console.error).finally(() => prisma.$disconnect());
