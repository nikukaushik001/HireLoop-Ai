const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const adminEmail = 'admin@hireloop.com'; await prisma.user.updateMany({ where: { email: { not: adminEmail } }, data: { role: 'RECRUITER' } }); const u = await prisma.user.findUnique({ where: { email: adminEmail } }); if (u) { await prisma.user.update({ where: { email: adminEmail }, data: { role: 'SUPERADMIN', isApproved: true } }); console.log('admin@hireloop.com upgraded to SUPERADMIN.'); } else { console.log('admin@hireloop.com does not exist in DB yet. Please register it.'); } console.log('Roles reset successfully.'); } main().catch(console.error).finally(() => prisma.$disconnect());
