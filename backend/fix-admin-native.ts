import { PrismaClient } from '@prisma/client'; import bcrypt from 'bcrypt'; const prisma = new PrismaClient(); async function main() { const email = 'admin@hireloop.com'; let user = await prisma.user.findUnique({ where: { email } }); if (!user) { console.log('Admin user not found. Creating...'); const hashedPassword = await bcrypt.hash('Admin123!', 10); user = await prisma.user.create({ data: { email, passwordHash: hashedPassword, name: 'Super Admin', role: 'SUPERADMIN', isApproved: true } }); console.log('Created admin user with password: Admin123!'); } else { console.log('Admin user found. Resetting password...'); const hashedPassword = await bcrypt.hash('Admin123!', 10); user = await prisma.user.update({ where: { email }, data: { passwordHash: hashedPassword, role: 'SUPERADMIN', isApproved: true } }); console.log('Reset admin user password to: Admin123!'); } } main().catch(console.error).finally(() => prisma.$disconnect());
