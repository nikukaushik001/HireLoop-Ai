const fs = require('fs');
const axios = require('axios');
const FormData = require('form-data');

async function testUpload() {
  const token = 'YOUR_BEARER_TOKEN'; // I need to login first or bypass
}
